SECRET_KEY=replace-me-with-a-secret
# Set to 'True' or 'False'
DJANGO_DEBUG=True
# Comma-separated origins allowed for CORS, e.g. http://localhost:3000
CORS_ALLOWED_ORIGINS=http://localhost:3000
# Optional: comma-separated allowed hosts for Django, e.g. localhost,127.0.0.1
ALLOWED_HOSTS=localhost,127.0.0.1
# Example sqlite url (defaults to sqlite:///db.sqlite3)
DATABASE_URL=sqlite:///db.sqlite3
# Copy to .env and adjust before running
DJANGO_DEBUG=True
SECRET_KEY=replace-me-with-a-secure-key
# Use sqlite file by default (file will be created in backend/)
DATABASE_URL=sqlite:///db.sqlite3
# Comma-separated origins allowed for CORS (DO NOT use *)
# Example: CORS_ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000
PORT=8000# Copy this file to `.env` and fill the values before running the app.
# Do NOT commit your real .env into git.

FLASK_ENV=development
SECRET_KEY=replace-me-with-secure-key
# Use sqlite file by default. For production set a proper database URL.
DATABASE_URL=sqlite:///backend.db
# Comma-separated origins allowed for CORS (DO NOT use *). Example:
# CORS_ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000

# App port (optional)
PORT=5000
